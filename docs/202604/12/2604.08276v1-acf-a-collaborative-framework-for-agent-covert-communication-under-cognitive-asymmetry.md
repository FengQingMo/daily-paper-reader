---
title: "ACF: A Collaborative Framework for Agent Covert Communication under Cognitive Asymmetry"
title_zh: ACF：一种解决认知不对称的智能体协作隐蔽通信框架
authors: "Wansheng Wu, Kaibo Huang, Yukun Wei, Zhongliang Yang, Linna Zhou"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08276v1"
tags: ["query:agent"]
score: 9.0
evidence: 提出了一种认知不对称下的智能体隐蔽通信协作框架。
tldr: 本文针对智能体隐蔽通信中的认知不对称问题，提出了非对称协作框架（ACF），通过将隐蔽通信与语义推理结构性解耦，构建前缀无关的解码机制。在动态智能体网络实验中，ACF在通信稳定性和信息保密性方面均显著优于传统对称方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08276v1/fig-001.webp\", \"caption\": \"Fig. 3. Trade-off analysis between semantic utility and channel reliability under retrieval-induced cognitive asymmetry (+RET).\", \"page\": 4, \"index\": 1, \"width\": 519, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.08276v1/fig-002.webp\", \"caption\": \"Fig. 2. Impact of controlled cognitive asymmetry on Bit Error Rate (BER). Asymmetry is explicitly governed by truncating varying numbers of historical sessions (∆) from the decoder’s context.\", \"page\": 3, \"index\": 2, \"width\": 522, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.08276v1/fig-003.webp\", \"caption\": \"Fig. 1. The Asymmetric Collaborative Framework (ACF). To overcome cognitive asymmetry (zenc t ̸= zdect ), ACF structurally decouples statistical communication from dynamic cognitive reasoning. Governed by a shared configuration Θ, ACF executes dynamic cognitive sampling and enables prefixindependent, model-free, and robust secret extraction (Π ⊥ zt).\", \"page\": 2, \"index\": 3, \"width\": 910, \"height\": 423}]"
motivation: 现有基于生成式AI的智能体隐蔽通信方法依赖认知对称，易受动态交互引起的前缀差异破坏同步。
method: 提出了一个由统计层和认知层正交分离的非对称协作框架，通过前缀无关的解码范式和共享隐写配置实现稳健通信。
result: 在认知不对称条件下，ACF在语义保真度和隐蔽通信性能上均优于对称基线，保持计算上不可区分性且具备可证明的误差界。
conclusion: ACF框架有效解决了智能体间认知不对称导致的隐蔽通信失效问题，显著提升了通信稳健性和保密性。
---

## 摘要
随着生成式人工智能的发展，自主智能体网络成为一种强大的交互式隐蔽通信范式。然而，由于智能体通过与环境的交互动态更新内部记忆，现有方法面临一个关键的结构性脆弱点：认知不对称。传统方法要求严格的认知对称性，即编码器与解码器之间存在完全相同的序列前缀。在动态部署中，前缀的不一致不可避免地破坏了同步，导致通信信道严重退化。为应对认知不对称这一核心挑战，我们提出了非对称协作框架（Asymmetric Collaborative Framework，ACF），该框架通过正交的统计层与认知层在结构上实现隐蔽通信与语义推理的解耦。通过引入基于共享隐写配置的前缀无关解码范式，ACF消除了对认知对称性的依赖。在基于真实记忆增强工作流的评估中，即使在严重认知不对称下，对称基线方法仍出现显著的信道退化，而ACF在语义保真度与隐蔽通信能力上均表现突出。它保持计算上的不可区分性，能以可证明的误差界限实现可靠的信息提取，并为现代智能体网络提供稳健的有效信息容量保证。

## Abstract
As generative artificial intelligence evolves, autonomous agent networks present a powerful paradigm for interactive covert communication. However, because agents dynamically update internal memories via environmental interactions, existing methods face a critical structural vulnerability: cognitive asymmetry. Conventional approaches demand strict cognitive symmetry, requiring identical sequence prefixes between the encoder and decoder. In dynamic deployments, inevitable prefix discrepancies destroy synchronization, inducing severe channel degradation. To address this core challenge of cognitive asymmetry, we propose the Asymmetric Collaborative Framework (ACF), which structurally decouples covert communication from semantic reasoning via orthogonal statistical and cognitive layers. By deploying a prefix-independent decoding paradigm governed by a shared steganographic configuration, ACF eliminates the reliance on cognitive symmetry. Evaluations on realistic memory-augmented workflows demonstrate that under severe cognitive asymmetry, symmetric baselines suffer severe channel degradation, whereas ACF uniquely excels across both semantic fidelity and covert communication. It maintains computational indistinguishability, enabling reliable secret extraction with provable error bounds, and providing robust Effective Information Capacity guarantees for modern agent networks.

---

## 论文详细总结（自动生成）

# ACF：一种解决认知不对称的智能体协作隐蔽通信框架  

---

## 一、研究核心问题与整体背景  

- **核心问题**：  
  本文聚焦于生成式人工智能驱动的智能体（LLM Agents）在隐蔽通信（Covert Communication）中面临的“认知不对称”（Cognitive Asymmetry）问题。  
  在传统隐写通信中，编码器与解码器需要保持“认知前缀对称”，即二者共享完全相同的上下文。动态环境中智能体不断更新内部记忆和状态，使得这种对称性不可能长期保持，从而导致通信信道同步崩溃、比特误差率（BER）急剧上升。  

- **研究动机**：  
  随着自律智能体系统的普及（如基于大型语言模型的多智能体网络），隐蔽通信成为确保隐私和安全的关键技术。然而这些模型具备环境感知与自我演化能力，其内部状态不断变化，传统方法无法适应这种动态不确定性。论文旨在在“认知不对称条件下”保持通信可靠性与语义一致性。  

- **整体含义**：  
  作者提出一个新的结构——**非对称协作框架（ACF）**，它通过统计层与认知层的**结构性解耦**实现稳健、同步无关的隐蔽通信，使智能体能在无前缀共享的条件下完成秘密信息的注入与提取。

---

## 二、方法论  

### 1. 核心思想  

- **结构性解耦（Structural Decoupling）**：  
  ACF将隐蔽通信分为两层：  
  - **统计通信层**：负责实际的秘密嵌入与解码。  
  - **认知推理层**：负责语义内容生成。  
  两者在数学上正交独立，使通信层不受认知状态变化影响。  

- **前缀无关解码范式（Prefix-Independent Decoding）**：  
  解码过程只依赖共享的隐写配置参数 Θ，而不需要模型或上下文 prefix，使得解码对认知状态的偏差具有鲁棒性。  

### 2. 关键技术细节  

- **认知不对称的形式化定义**：  
  作者形式化了智能体在动态状态下的前缀差异：  
  \[
  z_{enc}^{t} = K_{pub,enc}^{t} ∥ K_{priv,enc}^{t} \neq K_{pub,dec}^{t} ∥ K_{priv,dec}^{t} = z_{dec}^{t}
  \]  
  其中，公有与私有状态的不一致共同导致“认知不对称”。

- **词汇分区映射（Vocabulary Partitioning）**：  
  在共享配置 Θ=(sk, f, Pe, R) 下，通过伪随机映射 Π : V × Θ → {0,1} 将词汇表划分为两个子集 V(0) 和 V(1)。  
  这种分区前缀无关，确保嵌入时分布保持。  

- **分布置换嵌入（Distribution Permutation）**：  
  编码器根据当前语义分布 PM(· | z_enc^t) 计算累积分布函数（CDF），利用伪随机数 r ← G_sk 进行零失真置换嵌入：  
  \[
  x_t = CDF^{-1}_{s,Π}(r, P_M(· | z_{enc}^t))
  \]  
  保证语义概率分布不变，嵌入过程完全无扭曲。  

- **模型无关解码（Model-Free Extraction）**：  
  解码端根据序列 X=(x₁,…,x_T)和伪随机序列 r_t，通过统计聚合 Λ(X) 与阈值 τ(Pe,T) 二值判决恢复秘密比特：  
  \[
  \hat{s} = \begin{cases} 1 & \text{if } Λ(X) ≥ τ(P_e,T) \\ 0 & \text{otherwise} \end{cases}
  \]  
  从而实现基于假设检验的前缀无关秘密恢复。

---

## 三、实验设计  

### 1. 数据集与场景  

- 使用 **LongMemEval-s** 数据集评估长记忆对话能力。  
- 智能体模型选用 **Qwen2.5 7B Instruct**。  
- 设置三种场景来调控认知不对称程度：  
  1. **Isolated discrepancy**：解码端删除若干对话轮次；  
  2. **Progressive asymmetry**：逐步增加前缀缺失数量；  
  3. **Memory-augmented (+RET)**：编码端利用私有检索记忆池（115k tokens），形成极端状态差异。  

### 2. 对比方法  

- **Symmetric baselines**：DISCOP（基于分布复制的隐写）与 METEOR（基于密码分段的安全隐写）。  
- **评估指标**：  
  - 语义层：Gemini 2.0 Flash 作为评审模型，采用任务 F1 和语义得分。  
  - 通信层：比特错误率（BER）、生成熵（Entropy）、检测准确率（Detection Accuracy）、有效信息容量（EIC）等。

---

## 四、资源与算力  

- 文中仅提及所用模型（Qwen2.5 7B Instruct），并在公开仓库中提供推理代码。  
- **未说明具体GPU型号、计算数量或训练时长**，推测为纯推理级实验，无大规模训练环节。  
- 所有实验均在部署级智能体环境中运行，而非独立训练过程。

---

## 五、实验数量与充分性  

- 实验包括：  
  - 三种认知不对称情形（共 3 类场景）。  
  - 不同安全参数 k（8, 12, 16）对性能稳定性的测试。  
  - 对比三种方法（Normal / DISCOP / METEOR / ACF）。  
  - 指标维度涵盖语义与统计通信两类性能。  
- 共计至少 **9+组有效实验**（包含不同场景与参数组合），覆盖从轻度到极端不对称条件。  
- 实验设计系统完备，结果明确展示性能趋势，定量指标公正清晰，同类基线均为SOTA隐写算法，**公平性较高**。

---

## 六、主要结论与发现  

- **对称方法失效**：在认知不对称条件下，DISCOP/METEOR的BER高达约50%，EIC几乎为零。  
- **ACF稳定表现**：在同样的极端条件下，ACF（k≥12）实现0.00% BER，EIC稳定保持约1.18 bits/10³ tokens。  
- **语义性与隐蔽性双优**：ACF在维持真实语义的同时实现完全的计算不可区分性，检测准确率接近随机猜测（≈50%）。  
- **结构性解耦优势**：ACF有效解决了认知状态动态变化与通信可靠性之间的矛盾，实现首次真正可行的“异步智能体隐蔽信道”。

---

## 七、优点与创新亮点  

- **结构创新**：首次提出“统计通信层—认知推理层”的正交解耦结构。  
- **理论完备性**：方法具有可证明误差界和容量公式（基于 Shannon 定理）。  
- **实用性强**：兼容动态智能体框架（具备RAG、Memory等特性），无需冻结模型。  
- **隐蔽性强**：在对抗检测中保持近随机区分度，实证验证计算不可区分性。  
- **鲁棒性验证充分**：在多层认知不对称下仍保持通信稳定，适用于现实智能体网络中的命令与认证场景。

---

## 八、不足与局限  

- **算力未说明**：缺乏可复现的培训/推理时长与环境细节。  
- **容量相对较低**：在理想对称环境下传输率略低于传统方法，适合小比特任务而非高吞吐隐写。  
- **评估范围偏文本域**：尚未扩展至图像、音频或多模态隐写应用。  
- **未进行安全攻防实验**：论文中仅验证统计不可区分性，未包含主动攻击（如异常检测或模型逆向分析）测试。  
- **理论与实践结合度有限**：公式完备，但缺乏对大规模实际网络部署的性能验证。

---

（完）
