Title: Domain-Contextualized Inference: A Computable Graph Architecture for Explicit-Domain Reasoning

URL Source: https://arxiv.org/pdf/2604.04344v3

Published Time: Mon, 13 Apr 2026 00:20:39 GMT

Number of Pages: 25

Markdown Content:
# Domain-Contextualized Inference: A Computable Graph Architecture for Explicit-Domain Reasoning 

Chao Li 1, Yuru Wang 2, Chunyi Zhao 3

> 1

Deepleap.ai | lichao@deepleap.ai 

> 2

Northeast Normal University | wangyr915@nenu.edu.cn 

> 3

University of Otago | cccchunyi07@gmail.com 

Abstract 

We establish the computational theory of the Domain-Contextualized Concept Graph (CDC) framework, in which domain—the semantic context governing each reasoning operation—is an explicit first-class parameter. The central claim is that domain-explicit architecture yields computational properties unavailable when domain is implicit: pruning search space from O(N) to O(N/K), substrate-independent execution over symbolic, neural, vector, and hybrid substrates, and transparent inference chains in which every step carries its evaluative context. The computational theory is grounded in a domain algebra that is itself computable. The domain lattice admits a Heyting algebra structure (Theorem 4.9) derived from a prefix distributivity property on domain strings—a string operation, not an abstract axiom. Reindexing (knowledge inheritance) is formalized as a τ -typed Galois connection (Theorem 4.19) whose adjoint maps are the same lookup operations the inference engine executes. Analogical drift is algebraically necessary rather than an engineering artifact (Theorem 4.22). Neural convergence requires a rank-1 spectral contraction condition— ∥h_r ∥·∥ h_d ∥ < 1 —checkable at runtime from embedding norms without eigendecomposition (Theorem 8.4). Three domain computation modes are formalized with operational semantics: chain indexing as CSP arc-consistency, path traversal as Kleisli composition in the Domain Context Monad, and vector-guided computation as a substrate transition. Three minimal experiments illustrate the algebraic claims; a PHQ-9 case study demonstrates that all mechanisms correspond to identifiable operations in a real-world clinical reasoning task. The representational foundation is due to Li & Wang (2026a). The computational theory— operational semantics, complexity bounds, domain algebra, monad structure, substrate transitions, and boundary conditions—is the contribution of this paper. 

Keywords: inference engines, domain-contextualized reasoning, computable graphs, Heyting algebra, typed Galois connection, Kleisli composition, neuro-symbolic systems, knowledge representation 

1. Introduction 

1.1. Domain as a Computational Parameter 

We treat domain—the semantic context within which a reasoning operation is scoped—as an explicit computational parameter rather than a mere metadata attribute. This is a departure from standard 1

> arXiv:2604.04344v3 [cs.AI] 9 Apr 2026

practice in formal reasoning systems, where domain context is embedded in external query logic, application code, or implicit ontological commitments; it is not encoded in the reasoning unit itself. Human reasoning is irreducibly contextual. The concept "atom" means an indivisible particle in Dalton’s chemistry, a quantum field excitation in modern physics, and an uninterruptible operation in concurrent programming. This is not ambiguity—it is how cognition works. Concepts derive meaning from the frames within which they are understood (Fillmore, 1982; Gärdenfors, 2000). Formal reasoning systems have largely ignored this fact. When domain is implicit, the system cannot directly exploit it for pruning. Every query must consider all facts in the knowledge base. Cross-domain reasoning requires manual ontology alignment. These are not expressiveness limitations—they are organizational limitations. 

1.2. This Paper’s Scope 

The Domain-Contextualized Concept Graph (CDC) framework (Li & Wang, 2026a) establishes the representational foundation: a four-tuple ⟨concept, relation@domain, concept’ ⟩ in which domain is a modal world constraint grounded in Kripke-style possible-world semantics. The core representational unit is: 

τ = ⟨c, r @ d, c ′⟩

where c, c’ are concept nodes, r is a relation predicate, and d is a domain specification. The @

operator is a semantic scope operator that parameterizes the meaning of r by d.Prior work leaves the computational semantics uncharacterized: how reasoning propagates through the five-layer architecture, what the complexity bounds are, what monad structure underlies multi-hop traversal, and why the domain set’s algebraic structure makes these operations well-founded. This paper provides that characterization. We do not claim new logical expressiveness. The contribution is architectural: making domain an explicit computational parameter changes how reasoning is organized. The domain algebra (Section 4) is part of this computational theory—it proves that the architecture’s operations terminate, inherit correctly, and compose soundly. The algebra is not an independent mathematical excursion; every theorem in Section 4 corresponds directly to an operation the inference engine performs. 

1.3. Contributions Contribution 1. Five-layer computational architecture—Domain Lattice, Fiber Concept Graphs, Reindexing Functors, Cross-Fiber Bridges, and Meta-Layer—with characterized inter-layer interfaces and complexity bounds. The Reindexing Functor (Layer 3) is an independent contribution not present in Li & Wang (2026a). 

Contribution 2. Three domain computation modes with full operational semantics: chain indexing as CSP arc-consistency (O(k)); path traversal as Kleisli composition in the Domain Context Monad; vector-guided computation as a substrate transition enabling automatic bridge discovery. 

Contribution 3. A computable domain algebra grounding the architecture. The domain lattice is a Heyting algebra (Theorem 4.9) whose implication reduces to a longest-common-prefix 2computation. Reindexing is a τ -typed Galois connection (Theorem 4.19) whose adjoint maps are the inference engine’s own lookup operations. Analogical drift is algebraically necessary rather than an engineering artifact (Theorem 4.22). Neural convergence has a rank-1 norm condition checkable at runtime without eigendecomposition (Theorem 8.4). 

Contribution 4. Substrate-agnostic inference interface with three operations—Query, Extend, Bridge—admitting symbolic, neural, vector, and hybrid implementations with explicit semantic guarantees at each substrate level. 

Contribution 5. Four reliability conditions (C1–C4) with failure mode analysis, and a PHQ-9 case study validating all mechanisms against a real-world clinical reasoning task. 

1.4. Epistemic Status 

Throughout this paper we distinguish: theorems (complete proofs in Appendix A), conditional propositions (proven under stated assumptions), and conjectures (computationally motivated, empirically unverified). Section 8 marks each neural-substrate claim explicitly. 

1.5. Paper Organization 

Section 2 situates CDC within the reasoning systems landscape. Section 3 presents the five-layer architecture. Section 4 develops the domain algebra foundations. Section 5 analyzes domain computation modes. Section 6 establishes the substrate-agnostic interface. Section 7 presents core algorithms and complexity. Section 8 derives reliability conditions and failure modes. Section 9 presents the PHQ-9 case study. Section 10 discusses implications and open problems. 

2. Background and Related Work 

2.1. The Reasoning Systems Landscape First-order logic and Prolog. FOL is Turing-complete but satisfiability is undecidable in general. Prolog restricts to Horn clauses, achieving semi-decidability with SLD-resolution, but a predicate 

r(X, Y) has a single global interpretation; there is no native construct for domain-scoped semantic variation. 

Description logics and OWL. Decidability is achieved by restricting quantification. However, domain context is implicit: a class Atom in an ontology has a fixed interpretation, and representing the same concept under multiple contextual framings requires costly ontology alignment or URI disambiguation. 

Graph neural networks. R-GCN (Schlichtkrull et al., 2018) parameterizes message passing by relation type, learning separate weight matrices W_r per relation. Domain context is absent: W_r is global, with no mechanism for domain-conditioned semantic variation. 

Neuro-symbolic systems. Recent neuro-symbolic approaches (Garcez & Lamb, 2023; Mao et al., 2019) combine interpretability of symbolic reasoning with generalization of neural networks. CDC’s substrate-agnostic interface provides a natural architecture for this integration. 

Contextualized knowledge systems. Bozzato et al.’s (2018) CKR and Giunchiglia and 3Ghidini’s (2001) Local Model Semantics provide context-indexed knowledge bases with inheritance. CDC’s contribution relative to these is domain as a structural component of the four-tuple arity—not an external index—with explicit cross-domain bridge operations. 

The gap. None of these systems treats domain as an explicit computational parameter at the level of the reasoning unit, nor provides a formal algebraic theory of the domain set itself. CDC occupies a previously uncharacterized position in this design space. 

2.2. CDC Representational Foundation 

A CDC triple is a four-tuple τ = ⟨c, r, c’, d ⟩. Domain specifications are structured strings with recursive composition: 

> d := dimension | dimension@d

Examples: ’Physics’ , ’Physics@Quantum’ , ’HighSchool@Math@Calculus’ .The framework defines relation predicates in four groups: 

• Structural: is_a , part_of , has_attribute .

• Logical: requires , cause_of , enables , contrasts_with .

• Cross-domain: analogous_to , fuses_with .

• Temporal: evolves_to .The modal semantics are as follows: each domain d defines a possible world w_d in a Kripke frame ⟨W, R_acc, V ⟩. The assertion r(c, c’, d) is read as □_d r(c, c’) —the relation holds necessarily within world w_d .

3. The Five-Layer Computational Architecture 

3.1. Overview 

Layer Name Computational Role Key Operation L1 Domain Lattice Indexing and pruning Partial order traversal L2 Fiber Concept Graphs Intra-domain reasoning SLD-resolution L3 Reindexing Functors Cross-layer knowledge inheritance Monotone pullback L4 Cross-Fiber Bridges Inter-domain reasoning channels Morphism / Coproduct L5 Meta-Layer Self-describing inference rules Reflective query A single reasoning step may traverse multiple layers: a query enters at L2, triggers a domain inheritance check at L1 via L3, finds a cross-domain bridge at L4, and resolves applicable inference rules by consulting L5. 4L5 Meta-Layer   

> Relation semantics, monotonicity typing, reflective rule queries
> L4 Cross-Fiber Bridges
> Analogical mapping, fusion, inter-domain transfer channels
> L3 Reindexing Functors
> Selective inheritance from parent to child domains
> L2 Fiber Concept Graphs
> Intra-domain reasoning over CDC triples inside F(d)
> L1 Domain Lattice
> Domain indexing, pruning, specialization order, search control
> inheritance routing
> query dispatch
> cross-domain transfer
> predicate governance

Figure 1: Five-layer explicit-domain inference architecture. L1 indexes domain scope, L2 performs within-domain reasoning, L3 controls inheritance, L4 bridges fibers, and L5 governs relation semantics and reflective control. 

3.2. Layer 1: The Domain Lattice 

The domain lattice D is a partially ordered set where ≤ reflects domain specificity: 

> ′

P hysics @ Quantum ′ ≤′ P hysics ′ ≤ ⊤ 

More specific domains are lower in the lattice; the universal domain ⊤ is the top element. The domain lattice serves as the indexing structure for all CDC reasoning. Its primary computational role is pruning: when a query is issued with domain d, the search space is restricted to the fiber 

F(d) . In a knowledge base of N triples distributed across K domains of average size N/K, domain pruning reduces per-query search space from O(N) to O(N/K). The algebraic structure of this lattice is the subject of Section 4. 

3.3. Layer 2: Fiber Concept Graphs 

For each domain d, the fiber F(d) is a directed graph whose nodes are concepts and whose edges are relation predicate instances scoped to d:

F (d) = {⟨ c, r, c ′⟩| r(c, c ′, d )}

Reasoning within a fiber proceeds by SLD-resolution over Horn clause representations of CDC triples. The fiber is the primary locus of CDC computation. Key property (Domain Separation): facts in different fibers are semantically independent. r(c, c’ 1, d 1) and r(c, c’ 2, d 2) with c’ 1̸ =

c’ 2 and d1̸ = d2 are consistent. 

3.4. Layer 3: Reindexing Functors 

Layer 3 handles knowledge inheritance along the domain lattice. When a query in fiber F(d_child) 

cannot be resolved, the system may consult the parent fiber F(d_parent) .The inheritance mechanism distinguishes two predicate classes: 5• Monotone predicates: The truth of r(c, c’, d_parent) implies truth of r(c, c’, d_child) for all d_child ≤ d_parent . Taxonomic ( is_a ) and prerequisite ( requires )relations are monotone. 

• Non-monotone predicates: Truth in a parent domain does not propagate. Oppositional (contrasts_with ) and conflict relations are non-monotone. The reindexing functor F(d_parent → d_child) performs selective pullback: it transfers facts along monotone predicates while blocking non-monotone ones. Section 4 proves this is a τ -typed Galois connection (Theorem 6.4) and that the monotone/non-monotone distinction is algebraically necessary rather than a design choice (Corollary 6.5). 

3.5. Layer 4: Cross-Fiber Bridges 

analogous_to@D 1↔D2 establishes a structural correspondence between concepts in different fibers— formally, a partial morphism φ: F(D 1) → F(D 2) preserving some relational structure. The analogical bridge allows inference chains to transfer across domain boundaries. It does not assert identity or logical equivalence; it asserts structural similarity sufficient to support analogical inference. 

fuses_with@D 1+D 2 constructs a new domain D_new as the coproduct of D1 and D2. Unlike 

analogous_to , fuses_with modifies the domain lattice: D_new = D 1 + D 2 is added as a new node. This is a generative operation that can produce unbounded domain growth—a failure mode addressed in Section 8. 

3.6. Layer 5: The Meta-Layer 

Layer 5 is the self-descriptive layer. Relation predicates are represented as concept nodes and described using CDC triples:    

> is_a(requires, TransitiveRelation, 'Logic '). has_attribute(requires, monotone, 'Logic '). contrasts_with(requires, enables, 'Logic ').

The meta-layer drives the reindexing functor in L3 (the system queries L5 to determine predi-cate monotonicity) and enables runtime rule adaptation. This instantiates a reflective reasoning architecture: CDC can reason about its own reasoning rules. 

4. Domain Algebra Foundations 

This section establishes the algebraic structure that the five-layer architecture presupposes — and shows that this structure is itself computable. Every theorem here corresponds directly to an operation the inference engine performs: meet is a longest-common-prefix lookup, Heyting implication is a prefix-string query, reindexing adjoint maps are the L3 inheritance calls, and the neural contraction condition is a runtime norm check. The algebra proves these operations are well-founded; the architecture executes them. 64.1. Notation 

Symbol Meaning D Full domain set = D_obj ∪ D_meta D_obj Object-tier domains (govern concept relations) D_meta Meta-tier domains (govern relation properties) 

⊑ Specialization order: d 1 ⊑ d2 means d 1 is more specific 

⊓, ⊔ Meet and join in the domain lattice 

→ Heyting implication 

⊥, ⊤ Minimum (null) and maximum (universal) domain 

∆ Set of declared join generalizations F(d) Fiber: CDC triples scoped to domain d 

τ Monotonicity typing function: R → {monotone, non-monotone} 

α Abstraction map F(d_c) → F(d_p) (lower Galois adjoint) 

γ_τ τ -typed concretization / reindexing (upper Galois adjoint) 

π Projection functor D_meta → D_obj h_c, h_r, h_d Neural embeddings of concepts, relations, domains W_{r,d} Domain-conditioned weight matrix = h_r ⊗ h_d 

4.2. The Domain Lattice Definition 4.1 (Domain strings). D is generated by: d ::= dimension | dimension@d .

Definition 4.2 (Prefix order). d1 ⊑ d2 iff d2 is an initial segment of d1.

Definition 4.3 (Bounds). ⊤: universal domain, d ⊑ ⊤ for all d. ⊥: null domain, ⊥ ⊑ d for all d. A query result of ⊥ signals domain inconsistency. 

Definition 4.4 (Domain meet). d1 ⊓ d2 = longest common prefix, or ⊥ if none .

Two-layer join construction. The join requires care because domain strings form a prefix tree, not a simple total order. 

Base join (purely algebraic): d1 ⊔_base d 2 = least prefix shared by both in prefix order, or ⊤ if none . Always well-defined; no external ontology required. 

Declared generalization set ∆: A finite set of triples (d 1, d 2, d 3) ∈ D3 satisfying ∆-consistency: (i) d1 ⊑ d3 and d2 ⊑ d3; (ii) d3 is minimal among declared upper bounds; (iii) declarations are unambiguous. 

Enriched join: d1 ⊔_∆ d2 = d 3 if (d 1, d 2, d 3) ∈ ∆; otherwise = d 1 ⊔_base d 2.

Theorem 4.5 (D is a well-defined bounded lattice). For any ∆-consistent set (including ∆

= ∅), (D ∪ {⊥, ⊤}, ⊑, ⊓, ⊔_∆, ⊥, ⊤) is a bounded lattice. The algebraic structure does not depend on ∆; without ∆, the lattice uses ⊔_base throughout. (Proof: Appendix A.1) 

4.3. Heyting Algebra Structure Observation 4.6 (Global distributivity fails). With ’Physics’, ’Math’, ’Chemistry’ ⊑

’Science’ and ’Physics’ ⊓ ’Math’ = ⊥:7• LHS: ’Physics’ ⊓ (’Math’ ⊔ ’Chemistry’) = ’Physics’ ⊓ ’Science’ = ’Physics’ 

• RHS: (’Physics’ ⊓ ’Math’) ⊔ (’Physics’ ⊓ ’Chemistry’) = ⊥ ⊔ ⊥ = ⊥

LHS ̸ = RHS. The domain algebra is not a frame and not Boolean. 

Lemma 4.7 (Prefix Distributivity). For d, d 1, d 2 with d1 ⊓ d2̸ = ⊥:

d ⊓ (d1 ⊔b ased 2) = ( d ⊓ d1) ⊔b ase (d ⊓ d2)

Proof sketch: When d1 ⊓ d2̸ = ⊥, d1 ⊔_base d 2 = d 1 ⊓ d2 = p (common prefix is their base join). LHS = d ⊓ p = lcp(d, p) . Since p is a prefix of both d1 and d2, lcp(d, d 1) = lcp(d, d2) = lcp(d, p) , so RHS = lcp(d,p) . (Full proof: Appendix A.2) 

Definition 4.8 (Heyting implication). 

d1 → d2 = ⊔∆{d|d ⊓ d1 ⊑ d2}

Example: 

• ’Physics@Quantum’ → ’Physics’ = ⊤ (upward transfer is universal). 

• ’Physics’ → ’Physics@Quantum’ = ’Physics@Quantum’ (downward specialization requires already being in that context). 

Theorem 4.9 (Domain algebra is a Heyting algebra). For all a, b, c: 

a ⊓ b ⊑ c ⇐⇒ a ⊑ (b → c)

Proof uses Prefix Distributivity (Lemma 4.7) rather than global distributivity. The domain algebra is a Heyting algebra despite failing to be a frame. (Full proof: Appendix A.3) 

Corollary 4.10. Domain algebra is intuitionistic: d ⊔ ¬ d̸ = ⊤ in general (where ¬d := d →⊥). Domain reasoning is constructive. 

4.4. Connection to Kripke Semantics via Alexandrov Topology 

The Alexandrov topology on (D, ⊑) has open sets = upward-closed subsets; its open-set algebra 

Ω(D) is a complete Heyting algebra. 

Theorem 4.11. The canonical embedding ι: D → Ω(D) defined by ι(d) = ↑d preserves ⊓,

⊔, and maps d1 → d2 to the interior of {d’ | ↑d’ ∩ ↑ d1 ⊆ ↑ d2} in Ω(D) .

Corollary 4.12 (Connection to Kripke semantics). The Heyting implication d1 → d2

corresponds to the modal transfer condition □_{d 1} φ → □_{d 2} φ from Li & Wang (2026a), via the Alexandrov topology. Full alignment (Open Problem 1, Section 10) requires verifying R_acc = 

⊑ in the Kripke frame of Li & Wang (2026a). 8Relation class Typing       

> requires ,is_a ,part_of ,has_attribute monotone
> contrasts_with ,analogous_to non-monotone

Table 3: Representative values of the typing function τ .

4.5. Meta-Layer Stratification 

The meta-layer (L5) uses domains like ’Logic’ to govern relation properties, while object fibers use domains like ’Physics@Quantum’ for concept relations. These cannot coexist in a flat lattice without creating absurd inferences. 

Definition 4.13. D_obj : domains scoping r(c, c’, d) with c, c’ ∈ C. D_meta : domains scoping r(R, attr, d) with R ∈ R. D_obj ∩ D_meta = ∅. Each tier is independently a Heyting algebra satisfying the axioms of Definition 4.17. 

Definition 4.14 (Projection functor π). π: D_meta → D_obj maps each meta-tier do-main to its object-tier scope: π(’Logic’) = ⊤_obj ; π(’ICD11@Meta’) = ’ICD11’ . π is a lattice homomorphism. 

Definition 4.15 (Typing function τ ).       

> τ(R) = monotone if monotone ∈Prop(R, d_meta) for governing d_meta
> τ(R) = non-monotone otherwise

Proposition 4.16 (Commutativity). R is in the domain of γ_τ iff τ (R) = monotone iff monotone 

∈ Prop(R, d_meta) for some d_meta ∈ π−1(d_c) . This chain links meta-layer declaration → τ

→ domain of γ_τ .

4.6. Reindexing as Typed Galois Connection Definition 4.17. A typed Galois connection (α, γ_τ ) satisfies α(p) ⊑_Q q iff p ⊑_P γ(q) 

on the subcategory of edges r with τ (r) = monotone . On the non-monotone subcategory, γ_τ is undefined. 

Definition 4.18. α(⟨c, r, c’ ⟩_c) = ⟨c, r, c’ ⟩_p if declared in F(d_p) ; undefined otherwise. 

γ_τ (⟨c, r, c’ ⟩_p) = ⟨c, r, c’ ⟩_c if τ (r) = monotone ; undefined otherwise. 

Theorem 4.19 (Reindexing is a typed Galois connection). On the monotone subcategory: 

τc ⊑{ F (dc)}γτ (τp) ⇐⇒ α(τc) ⊑{ F (dp)}τp

(Proof: Appendix A.4) 

Corollary 4.20 (Monotone = closure-generating). τ (r) = monotone iff γ_τ ◦ α is a closure operator for r-edges. This corollary proves that the monotone/non-monotone distinction in Section 3.4 is a necessary consequence of typed Galois structure, not a design choice. 9Practical consequence: This resolves why contrasts_with(Wave, Particle, ’Physics’) 

should not be inherited into ’Physics@Quantum’ : wave-particle duality means the contrast relation does not hold in the quantum subdomain. γ_τ is simply undefined for non-monotone predicates, so no inheritance occurs. 

4.7. Cross-Domain Operations: Algebraic Grounding analogous_to. A partial lattice morphism φ: F(D 1) ⇀ F(D 2) is a partial function on dom( φ)

satisfying: partial meet/join preservation on dom( φ) and relational structure preservation (same 

τ -class). It does not require preservation of ⊥, ⊤, →.

Theorem 4.21. analogous_to(c 1, c 2, D 1, D 2) is well-founded iff there exists a partial lattice morphism φ: F(D 1) ⇀ F(D 2) with φ(c 1) = c 2.

Theorem 4.22 (Partial morphisms do not compose). φ12 : F(D 1) ⇀ F(D 2) and φ23 :F(D 2) ⇀ F(D 3) do not in general compose to a well-founded partial lattice morphism. 

Proof: The composition domain is dom( φ12 ) ∩ φ12 −1(dom( φ23 )) , strictly smaller than dom( φ12 )

in general. When φ12 (N(c)) ⊈ dom( φ23 ), the structural neighborhood of c is incompletely mapped. 

(Full proof: Appendix A.5) 

Corollary 4.23. Analogical drift (Failure Mode 3, Section 8.2) is algebraically necessary, not an engineering artifact. 

fuses_with. fuses_with(d 1, d 2) adds d_new with d1, d 2 ⊑ d_new and ht(D’) = ht(D) + 1. Under finite depth axiom A5, each invocation increases lattice height by exactly one, so at most 

H_max − ht(D 0) invocations are permitted before the bound is reached. This is an algebraically grounded bound, not an external counter. 

Proposition 4.24. fuses_with preserves Heyting structure: extend → to D’ by d → d_new = (d → d1) ⊔_∆ (d → d2), both computable in D. Adjointness (HA) holds by the same argument as Theorem 4.9. 

4.8. Complete Axiom System DA Definition 4.25 (Domain Algebra DA). (D ∪ {⊥, ⊤}, ⊑, ⊓, ⊔_∆, →, ⊥, ⊤) satisfying: 

• A1: ⊑ is a partial order 

• A2: Bounded: ⊥ ⊑ d ⊑ ⊤ 

• A3: ⊓ = lcp ; ⊔_∆ = enriched join (Definition 4.4) 

• A4: → satisfies (HA) (Definition 4.8) 

• A5 (Finite depth / C1): Every chain d1 ⊋ d2 ⊋ ... is finite 

• A6 (Prefix closure): d = a@d’ ∈ D implies a, d’ ∈ D

• A7 ( ∆-consistency): ∆ satisfies Definition 4.4 

• A8 (Lattice extension): fuses_with is a height-bounded lattice extension (Section 4.7) 10 5. Domain Computation Modes 

5.1. Mode 1: Chain Indexing 

In chain indexing, the domain string is used directly as a prefix filter. This is a CSP arc-consistency operation: the domain string acts as a constraint eliminating irrelevant variable bindings before search. 

> def query(concept, relation, domain_prefix): return [e for e in kb if e.concept == concept and e.relation == relation and e.domain.startswith(domain_prefix)]

Complexity: O(k) where k = |F(d)|. The 50 × speedup reported by Li & Wang (2026a) for a 100K-triple knowledge base across 50 domains follows directly from this O(N/K) bound. 

5.2. Mode 2: Path Traversal as Kleisli Composition 

In path traversal, concept nodes and domain nodes alternate:    

> c1->[r 1@d 1]-> c 2->[r 2@d 2]-> c 3-> ...

The domain may change between steps, triggering L3 reindexing or L4 bridge traversal. 

Kleisli structure. Path traversal admits a precise categorical interpretation as Kleisli composi-tion in the Domain Context Monad (Moggi, 1991). Define the monad T as: 

T (C) = P (C × D) − − thepowersetof concept − domainpairs 

A single reasoning step is a Kleisli arrow:     

> f_r,d : C →T(C) f_r,d(c) = (c ', d ') | r(c, c ', d) holds, d 'computed by reindexing from d

Kleisli composition ▷ sequences two such arrows: 

(f{r1, d 1} ▷ f {r2, d 2})( c) = ⋃

> {

(c2, d ′

> 2

) ∈ f{r1, d 1}(c)}f{r2, d ′

> 2

}(c2)

Monad laws. The Domain Context Monad satisfies left identity, right identity, and associativity. These laws guarantee that multi-hop reasoning chains can be decomposed and recomposed without changing their semantics, and that the reasoning trace is well-defined regardless of how intermediate steps are grouped. The original Prolog implementation of Li & Wang (2026a) approximates this through depth-first search but lacks the composability guarantees the Kleisli formulation provides. Complexity: A path of length L across domains d 1, ..., d_L has complexity O(L · max_i |F(d_i)|). With reindexing at each step: O(L · depth(D) · max_i |F(d_i)|). 11 5.3. Mode 3: Vector-Guided Computation 

In vector-guided computation, domain strings are embedded into a vector space, and domain relationships are computed by similarity rather than string matching. 

Characterization. Mode 3 is not a third independent logical mode parallel to Modes 1 and 2. It is a substrate transition enabling two operations that symbolic modes cannot support without manual specification: 

• Automatic bridge discovery: If sim(h_{D 1}, h_{D 2}) > θ, an analogous_to bridge between fibers is automatically activated. 

• Graded domain membership: A concept may belong to a domain with a degree rather than binary membership. 

CDC-GNN instantiation. Mode 3 enables the CDC-GNN variant, where the message passing formula becomes:      

> h_c '=σ(Σ_r,d mask_d * W_r,d * h_neighbor ) W_r,d = h_r ⊗h_d

The key distinction from R-GCN (Schlichtkrull et al., 2018) is W_{r,d} = h_r ⊗ h_d versus 

W_r . CDC-GNN conditions the transformation matrix jointly on relation and domain, enabling 

is_a@Physics and is_a@Biology to learn distinct semantic transformations. This parameterization is algebraically grounded: W_{r,d} is a rank-1 matrix, making spectral analysis tractable (Section 8 below). 

Semantic status. Results from Mode 3 are proposals, not assertions. Automatically discovered bridges require validation before entering the symbolic layer. The soundness condition is established in Theorem 9.3. 

6. The Substrate-Agnostic Inference Interface 

6.1. Interface Structure 

The CDC inference interface is defined by three operations: 

• Query(c, r, d) → {c’} : Return the set of target concepts satisfying the relation in the domain. 

• Extend(c, r, d, c’) : Assert a new CDC triple into the knowledge base. 

• Bridge(D 1, D 2) → φ: Return the cross-domain morphism between two fibers, if one exists. These operations are substrate-independent: their types are defined in terms of CDC concepts, relations, and domains, not execution mechanisms. The four-tuple ⟨c, r@d, c’ ⟩ is a floor, not a ceiling; additional parameters (temporal indices, confidence weights) can be incorporated at the application layer without modifying the interface. 12 Requirement Preferred substrate       

> Auditability required Symbolic
> Ambiguous natural language input Neural or hybrid
> Cross-domain bridge discovery Vector
> Zero hallucination constraint Symbolic or hybrid
> Large-scale pattern learning Neural
> Regulatory compliance (EU AI Act) Symbolic or hybrid

Table 4: Substrate selection as a deployment-time decision. 

6.2. Four Substrate Implementations Symbolic (Prolog). Query resolves by SLD-resolution over Horn clause representations. 

> query(C, R, D, C2) :- call(R, C, C2, D).

Reliability: complete over the knowledge base, no hallucination. Limitation: cannot handle ambiguous natural language input. 

Neural (GNN). Query resolves by message passing with domain-conditioned weight matrices 

W_{r,d} = h_r ⊗ h_d . Reliability: probabilistic output with generalization across unseen concepts. Limitation: opaque reasoning, hallucination risk. 

Vector. Query resolves by approximate nearest-neighbor search in domain-projected embedding space. Reliability: soft matching, handles linguistic variation. Limitation: semantic precision is approximate. 

Hybrid. Natural language input is processed by a neural component; output is verified against the symbolic substrate. This is the architecture validated in Prenosil et al. (2025): GPT-4 (neural) extracts candidate facts; a symbolic reasoner verifies them against medical rules.     

> Input →Neural(extract candidate facts)
> →Symbolic(verify against CDC rules)
> →Output (verified) or Human Review (discrepancy)

6.3. Substrate Selection 

The substrate-agnostic interface means these choices are made at deployment time, not at knowledge-base construction time. A knowledge base built for symbolic execution can be reused for neural execution without modification. 

7. Core Algorithms and Complexity 

7.1. Domain-Aware Transitive Closure 

> function transitive_closure(c, r, d): visited, result, queue = , [], [c]

13 while queue: current = queue.pop() if current in visited: continue visited.add(current) # L2: Query current fiber direct = query(current, r, d) # L3: Reindexing –- consult parent domains if r is monotone if is_monotone(r): # resolved via L5 meta-layer query for d_parent in parents(d): direct += query(current, r, d_parent) filtered to concepts in F(d) # L4: Bridge traversal –- follow analogous_to edges for (current, c_bridge, d, d_target) in bridges(current, d): bridged = query(c_bridge, r, d_target) for b in bridged: mark_as_hypothesis(b, derivation_depth=1) direct += bridged for target in direct: if target not in visited: result.append(target); queue.append(target) return result 

Complexity. Base traversal within a single fiber: O(|V_d| + |E_d|) with adjacency indexing. With reindexing across L levels: O(L · (|V_d| + |E_d|)). With B active L4 bridges: O((L + B) ·

(|V_d| + |E_d|)). Note: is_monotone(r) is a runtime query against L5, namely has_attribute(r, monotone, ’Logic’) , not a hardcoded property. Under condition C3 (meta-layer boundedness), this lookup is O(1) with indexing. 

7.2. Bidirectional Fixed-Point Iteration (Neural Substrate) 

function fixed_point_iteration(G, epsilon): initialize h_c, h_r randomly repeat: h_c_new = aggregate(W_r,d · h_neighbor for (c, r, c ', d) in G) # where W_r,d = h_r ⊗ h_d h_r_new = aggregate(h_c_source + h_c_target for (c, r, c ', d) in G) delta = max(||h_c_new - h_c||, ||h_r_new - h_r||) h_c, h_r = h_c_new, h_r_new until delta < epsilon return h_c, h_r 

Convergence (Theorem 8.4 applied). The joint update operator T is a contraction iff 

ρ(W_{r,d}) < 1 for all r, d. Since W_{r,d} = h_r ⊗ h_d is rank-1: 14 ρ(W{r, d }) = || hr|| 2 · || hd|| 2

Therefore T is a contraction iff ||h_r|| 2 · ||h_d|| 2 < 1 for all r, d — enforceable by spectral normalization. Crucially, this condition is checkable directly from embedding norms rather than requiring full eigendecomposition of the weight matrix, which is the structural advantage of the rank-1 parameterization over standard GNN weight matrices. Complexity per iteration: O(|E| · d2). Iterations to convergence: O(log(1/ ϵ)/log(1/ λ)) where λ

< 1 is the contraction factor. 

7.3. Cross-Domain Bridge Discovery 

> function discover_bridges(D1, D2, theta): bridges = [] for c1 in F(D1): for c2 in F(D2): sim = cosine(embed(c1, D1), embed(c2, D2)) if sim > theta: bridges.append(analogous_to(c1, c2, D1, D2)) return bridges

Complexity: O(|F(D 1)| · |F(D 2)| · d). Automatically discovered bridges are proposals, not assertions; the soundness condition is given in Theorem 9.3. 

7.4. Complexity Summary (from Domain Algebra) 

Operation Without DA With DA + pruning With DA + reindexing (depth L) Single lookup O(N) O(N/K) O(L ·N/K) Transitive closure O(N 2) O((N/K) 2) O(L ·(N/K) 2)Cross-domain bridge N/A O(k 1·k2) O(k 1·k2·L) 

Theorem 7.5. Under axiom A5, reindexing terminates in ≤ ht(D) steps. 

Theorem 7.6. Vector-discovered bridge analogous_to(c 1, c 2, D 1, D 2) is sound iff the simi-larity function approximates the partial lattice morphism condition of Theorem 4.21. 

8. Reliability Analysis 

8.1. Reliability Conditions 

CDC reasoning is sound and complete with respect to the knowledge base under: 

C1 (Domain lattice finiteness / Axiom A5). The domain lattice D has finite maximum depth. This bounds the reindexing traversal in L3 and prevents infinite inheritance chains. Algebraically, this is the finite depth axiom that makes the Galois connection (Section 4.5) and the height bound on fuses_with (Section 4.7) definite. 15 C2 (Predicate monotonicity specification). For each predicate used in cross-layer inheritance, its monotonicity status is explicitly declared in the meta-layer. The system trusts these declarations; incorrect specification may yield unsound inheritance. 

C3 (Meta-layer boundedness). The set of predicates described in the meta-layer is finite and stable. If new predicates are continuously added, the meta-layer query may not terminate. 

C4 (Acyclicity for transitive predicates). For predicates r that are both transitive and used in transitive closure computation, the fiber graph F(d) must be acyclic with respect to r. Cycles cause non-termination in Algorithm 7.1. 

8.2. Failure Modes Failure Mode 1: Domain proliferation via fuses_with .

fuses_with generates new domain nodes; applied iteratively, the domain lattice grows without bound, violating C1. Algebraically, each invocation increases ht(D) by 1; without a bound H_max ,the height is unbounded (equivalent to the halting problem in general). 

Detection: Monitor |D| during reasoning. If |D| exceeds threshold, halt and require human review. 

Mitigation: Require explicit authorization for fuses_with operations, or define H_max in the DA axiom system. 

Failure Mode 2: Cyclic prerequisites. 

If requires(A, B, d) and requires(B, A, d) both hold, Algorithm 7.1 enters an infinite loop, violating C4. 

Detection: Check acyclicity of the requires graph in F(d) before executing transitive closure. O(|V| + |E|). 

Mitigation: Reject cyclic requires assertions at knowledge-base construction time. 

Failure Mode 3: Analogical drift via transitive bridge chaining. 

Transitively chained analogous_to bridges: 

analogous to(A, B, D 1, D 2); analogous to(B, C, D 2, D 3) → analogous to(A, C, D 1, D 3)? 

may not preserve structural similarity. This is algebraically necessary by Theorem 4.22: the composed bridge loses structural fidelity because dom( φ12 ) ∩ φ12 −1(dom( φ23 )) is strictly smaller than dom( φ12 ).

Detection: Track bridge derivation depth; flag chains of depth > 1 as hypotheses. 

Mitigation: Do not automatically assert transitively derived bridges. 

8.3. Neural Substrate: Stability, Alignment, and Computable Guarantees Theorem 8.3 (Stability). Under the rank-1 contraction condition ∥h_r ∥2 · ∥ h_d ∥2 < 1 for all r, d, the CDC neural substrate has a unique stable fixed point (h_c*, h_r*) . (Proof: Appendix A.6) 

The contraction condition is not a theoretical hypothesis to be verified asymptotically — it 16 Property FOL Description Logic Prolog CDC                           

> Decidability Undecidable Decidable (restricted) Semi-decidable Decidable (C1–C4)
> Domain handling None Implicit None Explicit (structural)
> Cross-domain reasoning Manual Alignment required Manual Native (L4)
> Meta-reasoning Limited None Meta-interpreter Native (L5)
> Substrate independence N/A N/A N/A Yes
> Query complexity O(N) O(N) O(N) O(N/K)
> Algebraic theory N/A Partial N/A Heyting algebra

Table 6: Comparison of CDC with classical reasoning systems under single-column constraints. is a runtime norm check. Because W_{r,d} = h_r ⊗ h_d is rank-1, its spectral radius equals 

∥h_r ∥2 · ∥ h_d ∥2 exactly (the only nonzero singular value of a rank-1 matrix). Enforcing this bound via spectral normalization during training is a standard implementation step, and verifying it post-training requires only two vector norm computations per (r, d) pair. This is what makes the stability claim a computable guarantee rather than an approximation. 

Epistemic note. Theorem 8.3 guarantees numerical convergence only , not semantic correctness. Stability and correctness are separate claims; the following proposition makes the separation explicit. 

Proposition 8.4 (Semantic alignment — conditional). Assume training supervision: positive examples = symbolically derivable CDC triples; negative examples = closed-world negatives against the symbolic layer. Under this assumption, at the fixed point (h_c*, h_r*) : (i) embeddings respect the reindexing order — h_c(c, d_c) and h_c(c, d_p) for d_c ⊑ d_p are more similar to each other than to embeddings at incomparable domains; (ii) monotone relations encode transitivity constraints learned from meta-tier edges; (iii) γ_τ -derivable triples score higher than non-derivable triples. The assumption is computationally constructive: the symbolic layer generates the training signal, so alignment is a consequence of using the symbolic layer’s own outputs as supervision. The gap between stability and alignment is not philosophical — it is the question of whether the training data was built this way. When it is, Proposition 8.4 follows from the fixed-point structure. 

Conjecture 8.5 (Approximation quality). Under the training assumption of Proposition 8.4, 

precision( θ) = |I_neu( θ) ∩ I_sym| / |I_neu( θ)| → 1 as θ → 1. The algebraic grounding (rank-1 structure, typed reindexing, computable training signal) provides the basis for this conjecture; empirical quantification is deferred to future work. 

8.4. Comparison with Classical Systems 

9. Minimal Empirical Validation 

We include three minimal experiments to illustrate the algebraic claims. These are not performance evaluations; their purpose is to make the algebra visible. 17 Method is_a(Atom, Particle) inherited? contrasts_with(Wave, Particle) inherited?        

> Standard GNN (no τ)Yes Yes (incorrect)
> τ-typed reindexing Yes No (correct)

Table 7: Experiment 1: typed reindexing blocks semantically invalid inheritance.            

> Bridge SPR (average)
> φ12 (direct: NN →Brain) 0.75–0.85
> φ23 (direct: Brain →Social) 0.55–0.70
> φ13 (composed) 0.30–0.50 (significant degradation)

Table 8: Experiment 2: analogical drift intensifies under composed bridges. 

9.1. Experiment 1: Reindexing Correctness (Corollary 4.20) 

Algebraic claim: τ -typed reindexing correctly blocks non-monotone predicates while standard GNN message passing does not. 

Setup. Domain lattice with four levels: 

Top → Science → {Physics, Biology} → Physics@Quantum 

Concept nodes: Atom, Particle, Wave, Cell. Relations: is_a(Atom, Particle, ’Physics’) with 

τ = monotone , and contrasts_with(Wave, Particle, ’Physics’) with τ = non -monotone .Standard GNN propagates contrasts_with from ’Physics’ into ’Physics@Quantum’ —semantically wrong, since wave-particle duality makes this contrast inapplicable in the quantum subdomain. τ -typed reindexing blocks this by construction, because γ_τ is undefined for non-monotone predicates. 

Implementation: Python graph with 30 nodes, 5-domain hierarchy, pure symbolic computation. Runtime: minutes. 

9.2. Experiment 2: Analogical Drift (Theorem 4.22) 

Algebraic claim: Partial lattice morphisms do not compose; structural preservation degrades across bridge chains. 

Setup. Three domain fibers: D1 = Neural Networks (CS@ML), D2 = Brain Neuroscience (Biology@Neuro), D3 = Social Networks (Sociology@Networks). Metric: structural preservation rate SPR( φ, c) = fraction of structural neighborhoods correctly mapped through φ.The degradation is algebraically guaranteed by Theorem 4.22: dom( φ12 ) ∩ φ12 −1(dom( φ23 )) is strictly smaller than dom( φ12 ). The experiment measures the magnitude of this unavoidable loss. 

Implementation: Manually constructed partial morphisms, SPR by graph matching. Runtime: hours. 18 Condition Wr,d construction Spectral constraint                           

> A: Unconstrained Random W∈Rk×kNone
> B: Rank-1, unnormalized W=hr⊗hd, random hNone
> C: Rank-1, normalized W=hr⊗hd∥hr∥ · ∥ hd∥<1enforced
> Condition Behavior
> ADivergence or oscillation
> BSometimes converges, sometimes diverges
> CAlways converges

Table 9: Experiment 3: rank-1 normalization makes the contraction test explicit and stable. 

9.3. Experiment 3: Neural Contraction (Theorem 8.3 / 8.4) 

Algebraic claim: The rank-1 constraint ||h_r|| 2 · ||h_d|| 2 < 1 is both necessary and sufficient for convergence. 

Setup. Small CDC graph: 20 concept nodes, 3 relations, 2 domains. Three conditions: Condition A fails because arbitrary W matrices can have ρ(W) ≥ 1. Condition B shows rank-1 structure is necessary but not sufficient; normalization is also needed. Condition C shows ( ⋆) is sufficient: rank-1 structure makes the condition checkable from norms. 

Implementation: 50 iterations of message passing in PyTorch. Runtime: minutes. 

10. Case Study: PHQ-9 Clinical Reasoning 

10.1. Motivation 

The PHQ-9 (Patient Health Questionnaire-9) is a standardized instrument for depression screening, consisting of nine items corresponding to DSM-IV diagnostic criteria. We use PHQ-9 as a test case for the computational mechanisms described in Sections 3–9 rather than as an application demonstration. PHQ-9 has: a well-defined logical structure (diagnostic criteria are explicit), known reliability properties, and clear failure modes (under-reporting due to stigma, over-reporting due to distress). 

10.2. CDC Representation          

> % Fact layer: patient utterances cdc_fact(P001, 'lost_interest_in_activities ','@Psychology@PHQ9 ','Anhedonia '). cdc_fact(P001, 'poor_sleep_recently ','@Psychology@PHQ9 ','SleepDisturbance '). % Rule layer: diagnostic criteria cdc_rule( 'Anhedonia ','@Psychology@PHQ9@Item1 ', [ symptom_list: ["no longer enjoys previous activities", ...], frequency_scale: 0: "not at all", ..., 3: "nearly every day" ]). % Action layer: clinical process cdc_action(P001, 'PHQ9 ','r-score@Psychology@PHQ9 ', 14).

19 cdc_action(P001, 'PHQ9 ', 'r-severity@Psychology@PHQ9 ', 'moderate '). cdc_action(P001, 'SuicidalIdeation ', 'r-alert@Psychology@PHQ9 ', 'high '). 

The domain hierarchy @Psychology@PHQ9@Item1 instantiates a three-level fiber. Each level has distinct reasoning rules. 

10.3. Verification of Mechanisms Mode 1 (Chain indexing): A query for requires chains within @Psychology@PHQ9 retrieves only PHQ9-relevant prerequisites. CSP pruning reduces the search space from the full psychology knowledge base to the nine-item PHQ9 scope. 

Mode 2 (Path traversal / Kleisli): The full assessment path instantiates a Kleisli chain:         

> patient utterance →cdc_fact →cdc_rule matching →
> cdc_action (score) →cdc_action (severity) →
> cdc_action (alert, if SuicidalIdeation detected)

The chain is composable: intermediate results feed into subsequent steps without domain leakage. 

Algorithm 7.1 (Transitive closure): PHQ9 item dependencies are computed by transitive closure within the PHQ9 fiber. Acyclicity condition C4 holds: PHQ9 items have no circular dependencies. 

Failure mode verification: Item 9 (suicidal ideation) triggers an alert bypassing the normal scoring path. This is modeled as a conflict_with relation that activates the alert pathway. The system correctly identifies this as a non-monotone condition: an alert in the PHQ9 domain does not propagate to the general Psychology domain, consistent with Corollary 4.20. 

Reindexing (Theorem 4.19): General psychological prerequisites (e.g., conceptual under-standing of depression) are inherited from @Psychology into @Psychology@PHQ9 via the monotone reindexing functor. PHQ9-specific scoring rules are not propagated upward. The typed Galois connection ensures this asymmetry. 

10.4. The Silent Assessment Extension 

The standard PHQ-9 requires explicit patient cooperation. The CDC framework enables a natural extension: silent assessment, in which PHQ-9 scoring proceeds continuously over conversational input without explicit questionnaire administration. This extension introduces two computational problems: 

Problem 1: Utterance confidence. Reliability of a cdc_fact assertion derived from conversa-tion is lower than from explicit questionnaire response. We model this as a confidence weight w ∈

[0,1]:   

> score(item) = Σfrequency(symptom) * confidence(utterance) for symptoms mapped to item

20 Problem 2: Inconsistency detection. We model three types: logical (contradictory symptom assertions within a domain), state (temporal variation in symptom reports), and cognitive (mismatch between expressed affect and reported symptoms). Each has a distinct detection algorithm within the CDC framework. 

11. Discussion 

11.1. What This Paper Resolves 

The earlier version of this work identified domain algebra as an open problem. This paper closes it — and more importantly, closes it computably. The Heyting implication is a prefix-string operation (Definition 4.8). The τ -typed Galois connection’s adjoint maps are the inference engine’s L3 lookup calls (Theorem 4.19). The rank-1 contraction condition is a pair of vector norm checks (Theorem 8.4). Analogical drift is not a tuning artifact but an algebraically proven consequence of partial morphism non-composability (Theorem 4.22). The unified claim is: the domain algebra is the proof that the computational architecture does what it claims. The architecture is not justified by intuition or experiment alone; it is grounded in computable mathematics. 

11.2. Remaining Open Problems Open Problem 1 (Kripke alignment). Verify R_acc = ⊑ in the Kripke frame of Li & Wang (2026a) to complete the correspondence between modal and Heyting semantics (Corollary 4.12). 

Open Problem 2 (Automated τ verification). Can τ (R) be automatically verified from meta-tier CDC structure? Corollary 4.20 gives the algebraic criterion; implementation is feasible but not yet done. 

Open Problem 3 (Probabilistic domain algebra). Extend DA to confidence-weighted triples r(c, c’, d, w) . The domain algebra would become a quantale. 

Open Problem 4 (Temporal domain algebra). Extend to time-indexed fibers F(d, t) with 

evolves_to as morphisms between F(d, t) and F(d, t’) .

Open Problem 5 (Cross-substrate semantic equivalence). Formalize the conditions under which neural and vector substrate results are sound with respect to the modal semantics of Li & Wang (2026a). The informal characterization in Section 6.3 establishes the directions of approximation; precise conditions require a formal theory of approximate knowledge-base equivalence. 

11.3. Limitations 

Scale validation. The complexity analysis is theoretical. Empirical validation at scale (10 6+ triples, hundreds of domains) has not been performed. 

Domain proliferation. Failure Mode 1 has no complete algebraic solution; the height bound in DA is a necessary but not sufficient mitigation. 

Human-in-the-loop bottleneck. Bridge discovery and fuses_with require human validation before outputs can be trusted as assertions. At scale, the volume of proposals may exceed review capacity. 21 12. Conclusion 

We have presented the computational theory of Domain-Contextualized Concept Graphs: a five-layer inference architecture in which domain is a first-class computational parameter, characterized by three computation modes with operational semantics, a substrate-agnostic interface with four implementations, a computable domain algebra, and reliability conditions with explicit failure mode analysis. The architectural claim and the algebraic claim are the same claim viewed at different levels. Making domain an explicit computational parameter means the domain set must have a definite structure — and that structure turns out to be a Heyting algebra whose operations are computable string manipulations. Reindexing must be valid — and validity is exactly what the τ -typed Galois connection proves, using the same lookup the engine performs. Neural convergence must be checkable — and it is, from embedding norms alone, because the rank-1 structure of W_{r,d} = h_r ⊗ h_d 

makes the spectral radius algebraically transparent. The PHQ-9 case study demonstrates that these are not theoretical abstractions: each mechanism corresponds to an identifiable operation in a real clinical reasoning task, and the failure mode analysis reveals computational problems — silent assessment, inconsistency detection, dynamic questionnaire generation — that are natural within the CDC framework but invisible in standard questionnaire-based approaches. The representational foundation is due to Li & Wang (2026a). The computational theory developed here — operational semantics, complexity bounds, computable domain algebra, monad structure, substrate transitions, and boundary conditions — establishes what that foundation enables as a principled and mathematically grounded inference infrastructure. 

13. References 

Abramsky, S., & Jung, A. (1994). Domain theory. Handbook of Logic in Computer Science , Vol. 3. Banach, S. (1922). Sur les opérations dans les ensembles abstraits. Fundamenta Mathematicae ,3, 133–181. Barsalou, L. W. (1982). Context-independent and context-dependent information in concepts. 

Memory & Cognition , 10(1), 82–93. Birkhoff, G. (1940). Lattice Theory . AMS. Blackburn, P., de Rijke, M., & Venema, Y. (2001). Modal Logic . Cambridge University Press. Bordes, A., et al. (2013). Translating embeddings for modeling multi-relational data. NeurIPS ,26. Bozzato, L., Eiter, T., & Serafini, L. (2018). Enhancing context knowledge repositories with justifiable exceptions. Artificial Intelligence , 257, 72–126. Cousot, P., & Cousot, R. (1977). Abstract interpretation. POPL 1977 .Fillmore, C. J. (1982). Frame semantics. In Linguistics in the Morning Calm (pp. 111–137). Hanshin. Garcez, A. d’Avila, & Lamb, L. C. (2023). Neurosymbolic AI: The 3rd wave. Artificial Intelligence 

22 Review , 56(11), 12387–12406. Gärdenfors, P. (2000). Conceptual spaces: The geometry of thought . MIT Press. Ghidini, C., & Giunchiglia, F. (2001). Local models semantics, or contextual reasoning = locality + compatibility. Artificial Intelligence , 127(2), 221–259. Hogan, A., et al. (2021). Knowledge graphs. ACM Computing Surveys , 54(4), 1–37. Johnstone, P. T. (1982). Stone Spaces . Cambridge University Press. Klarman, S., & Gutiérrez-Basulto, V. (2016). Description logics of context. Journal of Logic and Computation , 26(3), 817–854. Kripke, S. A. (1963). Semantical considerations on modal logic. Acta Philosophica Fennica , 16, 83–94. Li, C., & Wang, Y. (2026a). Domain-constrained knowledge representation: A modal framework. 

arXiv:2604.01770 .Mac Lane, S. (1971). Categories for the Working Mathematician . Springer. Mao, J., et al. (2019). The neuro-symbolic concept learner. ICLR 2019 .McCarthy, J. (1993). Notes on formalizing context. IJCAI-93 , 555–560. Moggi, E. (1991). Notions of computation and monads. Information and Computation , 93(1), 55–92. Prenosil, G. A., Weitzel, T. K., et al. (2025). Neuro-symbolic AI for auditable cognitive information extraction from medical reports. Communications Medicine , 5, 491. Schlichtkrull, M., et al. (2018). Modeling relational data with graph convolutional networks. 

ESWC 2018 , 593–607. Sun, Z., et al. (2019). RotatE: Knowledge graph embedding by relational rotation in complex space. ICLR 2019 .Tarski, A. (1955). A lattice-theoretical fixpoint theorem. Pacific Journal of Mathematics , 5(2). Trouillon, T., et al. (2016). Complex embeddings for simple link prediction. ICML 2016 ,2071–2080. Yi, K., et al. (2018). Neural-symbolic VQA. NeurIPS 2018 .

14. Appendix A: Proofs 

14.1. A.1 Proof of Theorem 4.5 (Bounded Lattice) 

Proof. Well-definedness: ⊔_∆ is well-defined because ∆-consistency (3) forbids contradictory decla-rations, and ⊔_base is always well-defined. Meet axioms follow from symmetry and associativity of lcp. Join axioms: commutativity from symmetry of ∆ and lcp; associativity because each ⊔_∆

application produces the least upper bound, unique by ∆-consistency; absorption: d1 ⊔_∆ (d 1 ⊓

d2) = d 1 since d 1 is already above d1 ⊓ d2; idempotency: d ⊔_∆ d = d . ⊔_∆ is the least upper bound: upper bound by ∆-consistency (1) and ⊔_base construction; least by ∆-consistency (2). 23 14.2. A.2 Proof of Lemma 4.7 (Prefix Distributivity) 

Proof. When d1 ⊓ d2̸ = ⊥, d1 ⊔_base d 2 = d 1 ⊓ d2 = p (the common prefix is their base join). LHS = d ⊓ p = lcp(d, p) . RHS: d ⊓ d1 = lcp(d, d 1) and d ⊓ d2 = lcp(d, d 2). Since p is a prefix of both d 1 and d 2, lcp(d, d 1) and lcp(d, d 2) cannot extend past lcp(d, p) . Therefore 

lcp(d, d 1) = lcp(d, p) = lcp(d, d 2), and RHS = lcp(d,p) ⊔_base lcp(d,p) = lcp(d,p) 

= LHS. 

14.3. A.3 Proof of Theorem 4.9 (Heyting Algebra) 

Proof. (⇒) a ⊓ b ⊑ c implies a ∈ {d | d ⊓ b ⊑ c} , so a ⊑ ⊔ _∆{d | d ⊓ b ⊑ c} = b →

c.

(⇐) Let S = {d | d ⊓ b ⊑ c} and a ⊑ ⊔ _∆ S.

Step 1 (S is directed): For d, d’ ∈ S, both have common upper bound b → c = ⊔_∆ S, so d

⊓ d’ ̸ = ⊥ (elements with a common upper bound share a common prefix in the prefix lattice). Therefore S is directed under ⊔_base .

Step 2 (Prefix Distributivity applies): Since elements of S have pairwise common prefixes, Lemma 4.7 applies within S: (⊔_∆ S) ⊓ b = ⊔_∆{d ⊓ b | d ∈ S} .

Step 3: a ⊓ b ⊑ (⊔_∆ S) ⊓ b = ⊔_∆{d ⊓ b | d ∈ S} ⊑ c, where the last ⊑ holds be-cause each d ⊓ b ⊑ c by definition of S. 

14.4. A.4 Proof of Theorem 4.19 (Typed Galois Connection) 

Proof. (⇒) γ_τ (τ _p) defined ( τ (r) = monotone ). τ _c ⊑ γ_τ (τ _p) means τ _c is entailed by the concretized parent triple. α maps τ _c to parent fiber: α(τ _c) ⊑_{F(d_p)} τ _p by monotonicity of α.

(⇐) α(τ _c) defined, α(τ _c) ⊑ τ _p . By extensivity of γ_τ ◦ α (Lemma 2.6): τ _c ⊑

γ_τ (α(τ _c)) ⊑ γ_τ (τ _p) , using monotonicity of γ_τ and α(τ _c) ⊑ τ _p .

14.5. A.5 Proof of Theorem 4.22 (Partial Morphisms Do Not Compose) 

Proof. The composition domain is dom( φ12 ) ∩ φ12 −1(dom( φ23 )) , strictly smaller than dom( φ12 )

in general. When φ12 (N(c)) ⊈ dom( φ23 ), the structural neighborhood of c is incompletely mapped through the intermediate domain, violating Condition 3 (relational structure preservation) for the composed map. 

14.6. A.6 Proof of Theorem 8.3 / 8.4 (Neural Convergence) 

Proof. Banach fixed-point application. Sem_neu × Sem_neu under sup-norm is a Banach space. If T is a contraction with factor λ < 1, then by Theorem 2.10 it has a unique fixed point. 

Contraction condition. Operator norm of T_c is bounded by max_{r,d} ||W_{r,d}||_op = max_{r,d} ρ(W_{r,d}) . For rank-1 matrix W_{r,d} = h_r ⊗ h_d , the operator norm equals 

||h_r|| 2 · ||h_d|| 2 (the only nonzero singular value of a rank-1 matrix is the product of the norms of its factors). If ||h_r|| 2 · ||h_d|| 2 < 1 for all r, d, the maximum is < 1, giving contraction. If 24 any ρ(W_{r,d}) ≥ 1, the update can increase norms, violating contraction. 

Standard GNN comparison: W_r ∈ R^{k ×k} has no rank-1 structure; verifying ρ(W_r) < 1 requires full eigendecomposition. CDC’s tensor product parameterization makes the condition checkable from norms directly. 25